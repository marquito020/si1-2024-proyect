export enum PublicRoutes {
  LOGIN = "/login",
  REGISTER = "/register",
  LANDING = "/landing",
}

export enum PrivateRoutes {
  PRIVATE = "/",
  DASHBOARD = "/dashboard",
  HOME = "/home",
  USERS = "/users",
  EMPLEADOS = "/empleados",
  EMPLEADOCREATE = "/empleados/create",
  ROLES = "/roles",
  ROLECREATE = "/roles/create",
}

export const baseUrl = "http://localhost:4000/";